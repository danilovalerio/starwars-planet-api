#schema initialization
spring.jpa.hibernate.ddl-auto=update

#db
spring.datasource.url=${DB_URL}${DB_NAME}?useSSL=false
#se quiser pode usar o : (ou) no exemplo abaixo, que usa variável de ambiente, caso não encontre usa a url
#spring.datasource.url=${MYSQL_HOST:jdbc:mysql://localhost:3306/starwars?useSSL=false}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
